{% block title %}Register{% endblock %}

{% block body %}
    {% for flash_error in app.session.flashBag.get('verify_email_error') %}
        <div class="alert flash-message alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    {% for message in app.session.flashBag.get('success') %}
        <div class="alert flash-message alert-success" role="alert">{{ message }}</div>
    {% endfor %}

    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}


{# templates/auth/signup.html.twig #}
{% extends 'base.html.twig' %}

{% block bodyFullScreen %}
    {# Basic signup #}
    {{ include('registration/auth-signup-basic.html.twig') }}

    {# Cover signup (image / form) #}
    {# {{ include('registration/auth-signup-cover.html.twig') }} #}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- particles js -->
    <script src="/libs/particles.js/particles.js"></script>
    <!-- particles app js -->
    <script src="/js/pages/particles.app.js"></script>
    <!-- validation init -->
    <script src="/js/pages/form-validation.init.js"></script>
    <!-- password create init -->
    <script src="/js/pages/passowrd-create.init.js"></script>
{% endblock %}
