{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        {{ 
            component ('list-date-event',{ 
                users: users,
                events: events,
                nbAllEvents: nbAllEvents
            })  
        }} 

        {{ 
            component ('list-checkbox-event')  
        }} 
        
        {{ 
            component ('credit-card')   
        }}

        {{ 
            component ('fullcalendar',{ 
            events: events,
            })  
        }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}